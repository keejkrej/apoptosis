[dependency-groups]
dev = [
  "pytest>=8.3",
]

[tool.uv.workspace]
members = ["stain", "bf"]

[tool.uv.sources]
apoptosis-stain = { workspace = true }
apoptosis-bf = { workspace = true }

[tool.uv]
default-groups = ["dev"]
